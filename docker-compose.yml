services:
  web:
    build: 
      context: .
      dockerfile: .devcontainer/Dockerfile
    command: python /app/manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      - DEBUG=True